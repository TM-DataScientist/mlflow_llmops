# 対象ファイル
- scripts/web_ingest.py

# 指示
対象コードの動作を詳しく解説して

# 対象コード
    process = CrawlerProcess(
        settings={
            "LOG_LEVEL": "WARNING",
            "FEEDS": {
                "scraped_data.json": {
                    "format": "json",
                    "overwrite": True,
                },
            },
        }
    )


# その他指示
- ファイルを編集した場合には安全性を確かめた上で適切にコミットとプッシュを行って
- 指示が複雑な場合には"todo_{識別子}.md"を作成して、計画に沿ってタスクを完了してください。(複数のエージェントが動作するため、ユニークな識別子を付けてください。)
- 実施したことをCHANGE LOGに記載してください。

# ここはメモなので無視してください。
prompt.txtを読み込んで、記載された指示を実行してください。